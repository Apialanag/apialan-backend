# Variables de entorno para el ambiente de pruebas (Jest)

# Mercado Pago
MP_ACCESS_TOKEN="TEST-FAKE-ACCESS-TOKEN"
MP_SUCCESS_URL="http://localhost:5173/pago-exitoso"
MP_FAILURE_URL="http://localhost:5173/pago-fallido"
MP_NOTIFICATION_URL="http://localhost:3000/pagos/webhook"

# Base de datos (credenciales falsas, ya que la BD será mockeada)
DB_USER="test_user"
DB_HOST="localhost"
DB_DATABASE="test_db"
DB_PASSWORD="test_password"
DB_PORT=5432

# Secret para JWT (si es necesario para otras pruebas)
JWT_SECRET="test_secret_key"

# Email (credenciales falsas, ya que el servicio será mockeado)
EMAIL_USER="test@example.com"
EMAIL_PASS="test_password"
EMAIL_HOST="smtp.example.com"
EMAIL_PORT=587
EMAIL_FROM='"Test Service" <test@example.com>'
